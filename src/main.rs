// That's the dimensions
fn main() {
    let rect = (30, 60);

    println!("The area of the ractangle is {} square pixels.", area(rect))
}

fn area(dimesions: (u32, u32)) -> u32 {
    dimensions.0 * dimensions.1
}
// // That is simple function for multiply by
fn main() {
    let width1 = 30;
    let height1 = 50;

    println!(
        "the are in my own proparty the ractangle is {} square miter",
        are(width1, height1)
    )
}

fn are(width: u32, height: u32) -> u32 {
    width * height
}
